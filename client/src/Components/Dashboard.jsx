import React from "react"

const Dashboard = (props) => {
  const styles = {
    backgroundColor: 'lightblue', 
    padding: '20px',
    borderRadius: '10px', 
    textAlign: 'center', 
    color: 'white', 
    fontSize: '24px', 
  };

  return (
    <div style={styles}>
      User Dashboard
      
    </div>
  )
};

export default Dashboard;
